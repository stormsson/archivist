namespace Archivist.Generation.Sheets
{
    /// <summary>Three offices in v1, chosen because they disagree most (§5.1 of requirements).</summary>
    public enum Office
    {
        Hydrographic = 0,
        LandSurvey   = 1,
        Garrison     = 2
    }
}
